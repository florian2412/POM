'use strict';

/**
 * @ngdoc function
 * @name pomApp.controller:NavbarCtrl
 * @description
 * # NavbarCtrl
 * Controller of the pomApp
 */
angular.module('pomApp').controller('NavbarCtrl', function ($scope, $rootScope, $location, localStorageService) {
	
  //	$scope.user = localStorageService.get('currentUser').pseudo;
	
});