'use strict';

/**
 * @ngdoc function
 * @name pomApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the pomApp
 */
angular.module('pomApp')
  .controller('RegisterCtrl', function ($scope, $http, registerService) {
  
  });
